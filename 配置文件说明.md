# 配置文件说明

仓库根目录一共 9 个配置文件。名字看着都差不多，但性质差别挺大，所以先把最容易踩的坑说清楚：

**这 9 个文件里，只有 `config.yaml` 能直接丢给内核加载。剩下 8 个全是「覆写」——它们不是完整配置，单独打开会缺一堆东西，必须叠在订阅上面用。**

> **怎么记**：`config.yaml` 是底座，其余 8 个是往底座上糊的覆写料。
>
> **谁是母本**：`Full_control.*` 是最全的那份，我平时都拿它当模板改。`config_Override_Full.*` 就是拿它删掉 Twitch、Pixiv、Line、Discord 四组做出来的，顺手把 YAML 锚点也展开成了明文写法的。
> **YAML 和 JS 是同一份东西**：同一档位下的 `.yaml` 和 `.js` 内容等价，只是写法不同。`Full_control.yaml` 对 `Full_control.js`，`config_Override_Full.yaml` 对 `config_Override_Full.js`，lite 也是一样成对出现。

---

## 文件清单

| 文件 | 性质 | 策略组 | 说明 |
| --- | :---: | :---: | --- |
| `config.yaml` | 完整配置 | — | **唯一能直接加载的配置**。内置 `proxy-providers` 模板，同时也是三个 JS 脚本渲染时的基准 |
| `Full_control.yaml` | 覆写配置 | 37 | 最全的那份，写规则以它为准。保留了 YAML 锚点写法，改参数时省事 |
| `Full_control.js` | 覆写脚本 | 37 | 上面那份的 JS 版，锚点已经展开，给 Bettbox / Sparkle 用 |
| `config_Override_Full.yaml` | 覆写配置 | 33 | 比 `Full_control` 少了 Twitch / Pixiv / Line / Discord 四组，日常够用 |
| `config_Override_Full.js` | 覆写脚本 | 33 | 上一行的 JS 版 |
| `config_Override_lite.yaml` | 覆写配置 | 18 | 轻量款，手动维护，规则压到最少 |
| `config_Override_lite.js` | 覆写脚本 | 18 | 轻量款的 JS 版 |
| `SubConverter_config_Full.ini` | 转换配置 | — | 给 SubConverter 服务读的转换配置，全规则版。**不是覆写**，也不能加载进内核 |
| `SubConverter_config_lite.ini` | 转换配置 | — | 同上，轻量版 |

---

## 下载地址

下面每个文件都给两条链接，**内容完全一样，任选一条**：CDN 那条国内直连一般没问题，raw 那条是 GitHub 官方源。

| 文件 | CDN（推荐） | raw |
| --- | --- | --- |
| `config.yaml` | [`jsdelivr`](https://cdn.jsdelivr.net/gh/RikkaSaiko1/clash_config@main/config.yaml) | [`raw`](https://raw.githubusercontent.com/RikkaSaiko1/clash_config/main/config.yaml) |
| `Full_control.yaml` | [`jsdelivr`](https://cdn.jsdelivr.net/gh/RikkaSaiko1/clash_config@main/Full_control.yaml) | [`raw`](https://raw.githubusercontent.com/RikkaSaiko1/clash_config/main/Full_control.yaml) |
| `Full_control.js` | [`jsdelivr`](https://cdn.jsdelivr.net/gh/RikkaSaiko1/clash_config@main/Full_control.js) | [`raw`](https://raw.githubusercontent.com/RikkaSaiko1/clash_config/main/Full_control.js) |
| `config_Override_Full.yaml` | [`jsdelivr`](https://cdn.jsdelivr.net/gh/RikkaSaiko1/clash_config@main/config_Override_Full.yaml) | [`raw`](https://raw.githubusercontent.com/RikkaSaiko1/clash_config/main/config_Override_Full.yaml) |
| `config_Override_Full.js` | [`jsdelivr`](https://cdn.jsdelivr.net/gh/RikkaSaiko1/clash_config@main/config_Override_Full.js) | [`raw`](https://raw.githubusercontent.com/RikkaSaiko1/clash_config/main/config_Override_Full.js) |
| `config_Override_lite.yaml` | [`jsdelivr`](https://cdn.jsdelivr.net/gh/RikkaSaiko1/clash_config@main/config_Override_lite.yaml) | [`raw`](https://raw.githubusercontent.com/RikkaSaiko1/clash_config/main/config_Override_lite.yaml) |
| `config_Override_lite.js` | [`jsdelivr`](https://cdn.jsdelivr.net/gh/RikkaSaiko1/clash_config@main/config_Override_lite.js) | [`raw`](https://raw.githubusercontent.com/RikkaSaiko1/clash_config/main/config_Override_lite.js) |
| `SubConverter_config_Full.ini` | [`jsdelivr`](https://cdn.jsdelivr.net/gh/RikkaSaiko1/clash_config@main/SubConverter_config_Full.ini) | [`raw`](https://raw.githubusercontent.com/RikkaSaiko1/clash_config/main/SubConverter_config_Full.ini) |
| `SubConverter_config_lite.ini` | [`jsdelivr`](https://cdn.jsdelivr.net/gh/RikkaSaiko1/clash_config@main/SubConverter_config_lite.ini) | [`raw`](https://raw.githubusercontent.com/RikkaSaiko1/clash_config/main/SubConverter_config_lite.ini) |

> **该用哪条**：平时用 `cdn.jsdelivr.net`——它在全国各地都有缓存节点，访问快也稳，我自己的配置里所有远程资源（`.mrs` 规则集、图标）走的都是它。`raw.githubusercontent.com` 是 GitHub 的官方地址，没有缓存、内容永远是最新的，但国内直连基本打不开，得有代理才行。两条链接指向同一份文件，今天用这条明天用那条都无所谓。

---

## 三个规则量级怎么选

档位越往下规则越少，内存占用也越低。不确定就选中间的「全量精简」：

| 档位 | 配置文件 | 规则集 | 策略组 | 适用 |
| --- | --- | :---: | :---: | --- |
| 全量 | `Full_control.*` | 46 | 37 | 想给 Twitch / Pixiv / Line / Discord 这些也单独分流，机器内存不紧张 |
| 全量精简 | `config_Override_Full.*` | 42 | 33 | 常用服务基本都覆盖了，去掉的都是低频平台，**大多数人选这个就行** |
| 轻量 | `config_Override_lite.*` | 23 | 18 | 只要个能用的基础分流，路由器之类的小内存设备推荐 |

> 三行都是覆写，各有 YAML / JS 两份，得叠在订阅上用。**能单独加载的只有 `config.yaml`。**

---

## 两种覆写形态：YAML 与 JS

同一个档位我写了两遍，一份 YAML 一份 JS，效果一样，看你的客户端吃哪套：

| 形态 | 适用客户端 | 特点 |
| --- | --- | --- |
| **YAML 覆写**（`.yaml`） | Mihomo / Clash Meta 内核，且客户端支持 `rule-providers` 覆写 | 原生写法，能用 YAML 锚点（`<<: *rule_`）复用参数，改一处全生效 |
| **JS 覆写**（`.js`） | Bettbox / Sparkle 这类支持 JS 覆写的客户端 | 跑的是 `main` 函数，规则集参数由 `RULE_BASE` / `RULE_DOMAIN` / `RULE_IPCIDR` 三个工厂统一生成 |

对应关系：

- `Full_control.yaml` / `Full_control.js` → 37 策略组
- `config_Override_Full.yaml` / `config_Override_Full.js` → 33 策略组
- `config_Override_lite.yaml` / `config_Override_lite.js` → 18 策略组

### 覆写怎么用

`Full_control.*`、`config_Override_Full.*`、`config_Override_lite.*` 这 6 个文件都是覆写，导入方式看你客户端认哪种：

- **YAML 覆写**：客户端里有「覆写配置 / 覆写 YAML」之类的入口，导入对应的 `.yaml`
- **JS 覆写**：客户端里有「覆写脚本」入口，导入对应的 `.js`

| 档位 | YAML 覆写 | JS 覆写 |
| --- | --- | --- |
| 全量 | `Full_control.yaml` | `Full_control.js` |
| 全量精简 | `config_Override_Full.yaml` | `config_Override_Full.js` |
| 轻量 | `config_Override_lite.yaml` | `config_Override_lite.js` |

文件去哪下，翻回上面 [下载地址](#下载地址) 那张表就行。

### JS 覆写脚本（Bettbox / Sparkle）

三个 `.js` 就是上面三个 `.yaml` 的 JS 版，规则内容一样，只不过 YAML 里的锚点在这边换成了对象展开，你打开文件就能看到。

用法：

1. 把对应的 `.js` 下到本地
2. 客户端里进「覆写脚本」，导入这个文件
3. 到订阅 / profile 上打开「使用脚本覆写」，更新一次订阅就生效了

脚本的入口固定是 `const main = (config) => { ... }`。这里传进来的 `config` 是订阅已经合并好的完整配置对象，函数 return 回去的就是最终写进内核的东西——所以你能在函数里任意改它。

JS 版是**整段赋值**的语义：`config['dns'] = {...}` 就等于 YAML 里的 `dns!:`，写什么就是什么，整块替换掉。也因此 JS 能改的范围比 YAML 大一些，多出运行参数、`tun`、`sniffer`、`dns` 这几块：

| 覆写项 | YAML 版 | JS 版 |
| --- | :---: | :---: |
| `rule-providers` / `proxy-groups` / `rules` | 改 | 改 |
| 运行参数 / `tun` / `sniffer` / `dns`（含防泄漏配置） | 改 | 改 |
| `proxy-providers` / `proxies` / `mixed-port` / 认证 / `secret` / `external-controller` | 不碰，交给订阅和客户端自己管 | 同左 |

> 顺带提一句，JS 文件里不会有 `!` 和 `+` 后缀——那是 YAML 专有的，JS 里直接赋值就已经是整体替换了，没有「追加」这回事。

还有个好处：脚本头部内嵌了完整的「规则集 → 策略组」对照表（AI / 油管 / 谷歌 / 微软 / 苹果 / 电报 / 游戏平台 / 短视频 / 推特 / 图享 / 奈飞 / 影音 / 网盘 / 音乐 / 加密货币 / 图站 / 直播 / 插画 / 通讯 / 语音 / 代码托管），改规则前先翻一眼，不用再去猜某个流量最后会落到哪个组。

### ⚠️ 使用注意事项

这几点比较重要，用之前先看清楚：

- **只针对机场订阅**：脚本就是用来覆写机场下发的配置的，**别拿它去覆写你自己手写的配置**——它会整段重写 `rule-providers` / `proxy-groups` / `rules` / `dns`，你手写的东西会被直接盖掉。
- **关掉代理软件的 DNS 覆写**：脚本已经处理了机场私有 DNS、节点域名 `hosts` 映射的解析问题。再叠一层客户端的 DNS 覆写会打架，所以**把代理软件自带的 DNS 覆写关掉**。
- **Windows 防 DNS 泄露**：Windows 默认的「智能多宿主名称解析」可能绕过代理，造成 DNS 泄露。要彻底解决，二选一：
  - 关掉系统的「智能多宿主名称解析」（Smart Multi-Homed Name Resolution，注册表 / 组策略可关）；或
  - 在代理软件里开启 [严格路由](https://wiki.metacubex.one/config/inbound/tun/#strict-route)（即 `tun.strict-route`）。

---

## SubConverter 转换配置

`SubConverter_config_Full.ini` / `SubConverter_config_lite.ini` 是给 SubConverter 服务读的**订阅转换配置**。它们既不是覆写，也不会被加载进内核——用途完全不同。

什么时候会用到：客户端压根不支持覆写（或者覆盖写不生效），只能走订阅转换链接。

自建 SubConverter 的话，大概这么几步：

1. 先把 [SubConverter](https://github.com/tindy2013/subconverter) 部署起来
2. 把 `.ini` 丢进 SubConverter 的 `config` 目录
3. 转换链接上用 `config` 参数指定配置名

   ```
   https://你的域名/sub?target=mihomo&url=订阅链接&config=https://raw.githubusercontent.com/RikkaSaiko1/clash_config/main/SubConverter_config_lite.ini
   ```

---

## 使用方式速查

| 场景 | 用法 |
| --- | --- |
| 全新开箱 | 直接加载 `config.yaml`，把里面的订阅地址换成自己的就行 |
| 已有订阅，想叠加分流 | 把想要的 `Full_control.*` / `config_Override_*.*` 当覆写叠到订阅上 |
| 客户端不支持覆写 | 走 `SubConverter_config_*.ini`，用订阅转换 |

### config.yaml 起手式

其实只需要改 `proxy-providers` 里的订阅地址，其他字段保持默认就能跑：

```yaml
proxy-providers:
  节点1:                  # 订阅名称，可自定义，不可重复
    url: "你的订阅链接"    # ← 替换为你的订阅地址
    type: http
    interval: 1800        # 订阅自动更新间隔（秒）
    health-check:
      enable: true
      url: "https://www.gstatic.com/generate_204"
      interval: 100       # 节点健康检查间隔（秒）
    override:
      additional-prefix: "[]"  # 节点名称前缀
```

想挂多个订阅，就把 `节点1` 整块复制一份，改成 `节点2`、`节点3`……**名字别重名**，其余字段照搬即可。上面这几项（`interval`、`health-check`、前缀）都按自己习惯调，注释里写了单位。
